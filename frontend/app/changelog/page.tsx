import PlaceholderPage from '@/components/PlaceholderPage';
import { Activity } from 'lucide-react';

export default function ChangelogPage() {
  return (
    <PlaceholderPage
      title="Changelog"
      description="Stay updated with the latest features, improvements, and bug fixes. See what's new in every release."
      icon={Activity}
    />
  );
}
